<Window x:Class="ApiTester.ImportCurlDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Import cURL Command" Height="400" Width="600"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" 
                   Text="Paste your cURL command here:" 
                   FontWeight="Bold" 
                   Margin="0,0,0,10"/>

        <TextBox Grid.Row="1"
                 Name="CurlTextBox"
                 AcceptsReturn="True"
                 TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"
                 FontFamily="Consolas"
                 Padding="5"/>

        <StackPanel Grid.Row="2" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right" 
                    Margin="0,10,0,0">
            <Button Content="Import" 
                    Click="Import_Click" 
                    Width="100" 
                    Padding="5"
                    Margin="5"
                    Background="#007ACC"
                    Foreground="White"
                    BorderThickness="0"/>
            <Button Content="Cancel" 
                    Click="Cancel_Click" 
                    Width="100" 
                    Padding="5"
                    Margin="5"/>
        </StackPanel>
    </Grid>
</Window>
